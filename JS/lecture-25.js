// ! Lecture-25
// ? export default and named export

// * named exports - exports multiple things(variables,functions etc.) with their names
// * export one main thing from the file (to one single place)

// ? export default keywords used to export a single main function, whereas named export is to export multiple functions in/from a file

// ? Helper functions usecase-named export - named export commonly used in util files, where you are using a lot of helper functions and you are reusing them at multiple places(files)
